<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Apotheca</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="<%= items_path %>">Items</a>
        </li>
        <% if can? :manage, User %>
          <li class="nav-item">
            <a class="nav-link" href="<%= users_path %>">Users</a>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="<%= alert_messages_path %>">Alerts</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to "#{bs_icon :person} Sign Out".html_safe, destroy_user_session_path, class: 'btn' %>
          </li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>
